server:
  port: 8080

spring:
  application:
    name: ci-banking-backend

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

cibanking:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:*}
